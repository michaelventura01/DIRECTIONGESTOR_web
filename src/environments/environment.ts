// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,

  firebase: {
    apiKey: 'AIzaSyCjba3kEl27Wyr87nvs28EwK4vJvi8ruCE',
    authDomain: 'direction-gestor.firebaseapp.com',
    databaseURL: 'https://direction-gestor.firebaseio.com',
    projectId: 'direction-gestor',
    storageBucket: 'direction-gestor.appspot.com',
    messagingSenderId: '615191534887',
    appId: '1:615191534887:web:737ee982b2298f4c4a1d02',
    measurementId: 'G-7M8DBFLX3Y'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
